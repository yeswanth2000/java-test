pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build'
                sh 'mvn package'
            }
        }

        stage('Push') {
            steps {
                echo 'Build'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Build'
            }
        }
    }
}