pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build'
            }
        }

        stage('Push') {
            steps {
                echo 'Build'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Build'
            }
        }
    }
}